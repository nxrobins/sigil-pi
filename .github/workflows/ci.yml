name: CI

# sigil-pi's real gate is ./ci.sh, and ci.sh cannot run without a built SIGIL
# toolchain (every tool is forged by sigil-mcp/sigil-serve from a sibling
# checkout). That splits CI in two:
#
#   standalone — everything that needs no toolchain. Runs on every PR, today.
#   forge      — the full ./ci.sh gate. Needs SIGIL, so it is gated on a
#                token and shows as SKIPPED when one is not configured. The
#                gating is at the JOB level on purpose: a step-level gate
#                reported a green "success" in ~7s while running nothing,
#                which is worse than no check — a skipped job says what it
#                did (nothing), a green one lies.
#
# TO ENABLE THE REAL GATE: add a repo secret SIGIL_REPO_TOKEN with read
# access to nxrobins/SIGIL (the repo is private). The `ref` in SIGIL_REV is
# already pushed and the tree-hash check inside ci.sh stays the authority —
# the ref is only how CI fetches a candidate.

on:
  # No `branches:` filter on pull_request. That filter matches the BASE branch,
  # so `branches: [main]` silently gives ZERO checks to any stacked PR (one
  # based on another branch) — a check that does not check, which is worse than
  # no check at all. Found the first time a PR was stacked here. Run on every
  # PR regardless of base; push stays scoped to main so merges are verified once.
  pull_request:
  push:
    branches: [main]

jobs:
  standalone:
    name: standalone (no toolchain)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Every Python file parses
        run: python -m compileall -q agent.py chat.py drive.py make_chat_turn.py tests

      - name: SIGIL_REV is well-formed
        # A malformed pin silently disables the toolchain check in ci.sh, which
        # is exactly the failure this file exists to prevent.
        run: |
          python - <<'PY'
          import re, sys
          from pathlib import Path
          cfg = {}
          for line in Path("SIGIL_REV").read_text().splitlines():
              line = line.split("#", 1)[0].strip()
              if "=" in line:
                  k, v = line.split("=", 1)
                  cfg[k.strip()] = v.strip()
          missing = [k for k in ("crates", "stdlib") if k not in cfg]
          if missing:
              sys.exit(f"SIGIL_REV is missing required key(s): {missing}")
          for k in ("crates", "stdlib"):
              if not re.fullmatch(r"[0-9a-f]{40}", cfg[k]):
                  sys.exit(f"SIGIL_REV `{k}` is not a 40-char git tree hash: {cfg[k]!r}")
          print("SIGIL_REV ok:", {k: cfg[k][:12] for k in ("crates", "stdlib")})
          PY

      - name: ci.sh is executable and syntactically valid
        # ci.sh is `#!/bin/zsh` and ubuntu-latest does not ship zsh. Check it
        # with the shell that actually runs it — validating with bash instead
        # would be a weaker check that could pass on syntax zsh rejects.
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq zsh
          test -x ci.sh || { echo "ci.sh is not executable"; exit 1; }
          zsh -n ci.sh

  gate:
    # Job-level `if:` cannot read `secrets`, so this tiny job probes for the
    # token and publishes the answer; `forge` keys its OWN job-level `if` off
    # the output and is SKIPPED — visibly — when the toolchain is unreachable.
    name: is the SIGIL toolchain available?
    runs-on: ubuntu-latest
    outputs:
      available: ${{ steps.check.outputs.available }}
    steps:
      - name: Probe for SIGIL_REPO_TOKEN
        id: check
        run: |
          if [ -n "${{ secrets.SIGIL_REPO_TOKEN }}" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "::notice::SIGIL_REPO_TOKEN is not configured, so the real gate" \
                 "(./ci.sh) is SKIPPED here. Run it locally. See the comment at" \
                 "the top of .github/workflows/ci.yml to enable it."
          fi

  forge:
    name: full ./ci.sh gate (needs SIGIL)
    needs: gate
    if: needs.gate.outputs.available == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: sigil-pi

      - name: Read the pinned ref
        id: pin
        run: |
          ref=$(sed -n 's/^[[:space:]]*ref[[:space:]]*=[[:space:]]*//p' sigil-pi/SIGIL_REV | head -1)
          if [ -z "$ref" ]; then
            echo "SIGIL_REV has no \`ref\` key — CI needs one to know what to check out." >&2
            exit 1
          fi
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - uses: actions/checkout@v4
        with:
          repository: nxrobins/SIGIL
          token: ${{ secrets.SIGIL_REPO_TOKEN }}
          ref: ${{ steps.pin.outputs.ref }}
          path: SIGIL

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: SIGIL

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Create the venv ci.sh expects
        working-directory: sigil-pi
        run: |
          python -m venv .venv
          .venv/bin/pip install --quiet --upgrade pip
          .venv/bin/pip install --quiet pytest hypothesis

      - name: ./ci.sh
        # ci.sh builds the forge binaries itself (at the pin — issue #6), so
        # there is no separate build step to drift out of sync with it.
        working-directory: sigil-pi
        env:
          SIGIL_ROOT: ${{ github.workspace }}/SIGIL
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq zsh
          ./ci.sh
